<h2 align=center> Category: <%= @category.name %>  </h2>
<% if logged_in? and current_user.admin? %>
  <div class="center">
    <%= link_to "Edit Category", edit_category_path(@category), style: "color:green; font-size:1.8rem" %>
  </div>
<%end %>
<div class="center">
  <%= will_paginate @category_articles %>
</div>
<%= render 'articles/articles', obj: @category_articles %>
<div class="center">
  <%= will_paginate @category_articles %>
</div>
